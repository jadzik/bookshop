%h2 Załóż konto 
= form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f|
  = devise_error_messages!
  .clearfix
    = f.label :name, "Imię i nazwisko lub nazwa firmy*"
    .input= f.text_field :name
  .clearfix
    = f.label :client_type, "Wybierz typ klienta:*"
    .input= f.select :client_type, ["indywidualny", "firma", "biblioteka", "szkoła"]
  .clearfix
    = f.label :email, "Adres e-mail*"
    .input= f.email_field :email
  .clearfix
    = f.label :password, "Hasło*"
    .input= f.password_field :password
  .clearfix
    = f.label :password_confirmation, "Potwierdź hasło*"
    .input= f.password_field :password_confirmation
  .clearfix
    Musisz podać conajmniej jeden z numerów:*
    %br/
    = f.label :stationary_phone, "Numer stacjonarny"
    .input= f.text_field :stationary_phone
  .clearfix
    = f.label :mobile_phone, "Numer komórkowy"
    .input= f.text_field :mobile_phone
  .clearfix
    %br/
    Poniższe pola wypełniają tylko firmy, szkoły i biblioteki:
    %br/
    = f.label :fax_phone, "Fax"
    .input= f.text_field :fax_phone
  .clearfix
    = f.label :nip, "Numer NIP*"
    .input= f.text_field :nip
  .clearfix
  %br/
  Klikając "Zarejestruj się" akceptujesz #{link_to "regulamin", rules_path}.
  .clearfix.actions
    = f.submit "Zarejestruj się", :class => "btn primary"
%br/
*Pola oznaczone gwiazdką są wymagane
