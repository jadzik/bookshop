- if @client.errors.any?
  %div.alert-message.block-message.error
    %b Wystąpiły następujące błędy:
    - @client.errors.each do |attr, msg|
      %li
        = Pole
        %strong
          = t(attr)
        = msg
%br/

